import math  # Importamos el módulo math para usar el valor de pi

# Pedimos al usuario que ingrese el radio del cilindro
radio = float(input("Ingrese el radio del cilindro: "))

# Pedimos al usuario que ingrese la altura del cilindro
altura = float(input("Ingrese la altura del cilindro: "))

# Calculamos el volumen usando la fórmula: volumen = π * r^2 * h
volumen = math.pi * (radio ** 2) * altura

# Mostramos el resultado al usuario
print(f"El volumen del cilindro es: {volumen}")
